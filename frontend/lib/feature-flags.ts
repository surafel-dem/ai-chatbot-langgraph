export const featureFlags = {
  // Enables read-only anonymous session cookie + future credits enforcement
  credits: true,
  // Enables orchestrator agents (router → planner → specialists) and UI panels
  agentsOrchestrator: false,
};


